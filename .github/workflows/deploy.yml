name: Cofre Digital

on:
  push:
    branches:
      - main

jobs:
  teste:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Mensagem
        run: echo "Workflow executado com sucesso!"
